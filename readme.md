This is a sample readme.
I have used git fetch that updates an existing local repository with the latest changes from the remote server without altering your working files

git pull downloads remote changes and immediately merges them into your local files, while git fetch only downloads the changes without altering your local work.

Now I did reverse send local data onto server.